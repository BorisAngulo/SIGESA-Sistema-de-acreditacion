# Variables de entorno para Docker - SIGESA
# Copia este archivo como .env en la raíz del proyecto

# === CONFIGURACIÓN DE LA APLICACIÓN ===
APP_NAME="SIGESA"
APP_ENV=production
APP_KEY=base64:tu_clave_aqui_generada_con_artisan_key_generate
APP_DEBUG=false
APP_URL=http://localhost

# === CONFIGURACIÓN DE BASE DE DATOS POSTGRESQL ===
DB_CONNECTION=pgsql
DB_HOST=postgres
DB_PORT=5432
DB_DATABASE=sigesa_db
DB_USERNAME=sigesa_user
DB_PASSWORD=sigesa_secure_password_2024

# === CONFIGURACIÓN DE REDIS ===
REDIS_HOST=redis
REDIS_PASSWORD=sigesa_redis_secure_pass_2024
REDIS_PORT=6379

# === CONFIGURACIÓN DE CORREO ===
MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=tu_email@gmail.com
MAIL_PASSWORD=tu_password_de_aplicacion
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS="noreply@sigesa.edu.bo"
MAIL_FROM_NAME="${APP_NAME}"

# === CONFIGURACIÓN DE DOCKER ===
# Puertos externos
FRONTEND_PORT=3000
HTTP_PORT=80
HTTPS_PORT=443

# URLs para el frontend
REACT_APP_API_URL=http://localhost:8000/api
REACT_APP_APP_NAME=SIGESA

# Configuraciones de inicio
RUN_MIGRATIONS=true
RUN_SEEDERS=false

# === CONFIGURACIÓN DE ARCHIVOS ===
FILESYSTEM_DISK=local

# === CONFIGURACIÓN DE SANCTUM ===
SANCTUM_STATEFUL_DOMAINS=localhost,localhost:3000,127.0.0.1,127.0.0.1:3000
SESSION_DOMAIN=localhost

# === CONFIGURACIÓN DE CORS ===
FRONTEND_URL=http://localhost:3000

# === CONFIGURACIÓN DE LOGS ===
LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# === CONFIGURACIÓN DE BROADCASTING ===
BROADCAST_DRIVER=log

# === CONFIGURACIÓN DE CACHE ===
CACHE_DRIVER=redis
FILESYSTEM_DISK=local
QUEUE_CONNECTION=redis
SESSION_DRIVER=redis
SESSION_LIFETIME=120

# === CONFIGURACIÓN AWS (SI SE USA) ===
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_DEFAULT_REGION=us-east-1
# AWS_BUCKET=
# AWS_USE_PATH_STYLE_ENDPOINT=false

# === CONFIGURACIÓN DE PUSHER (SI SE USA) ===
# PUSHER_APP_ID=
# PUSHER_APP_KEY=
# PUSHER_APP_SECRET=
# PUSHER_HOST=
# PUSHER_PORT=443
# PUSHER_SCHEME=https
# PUSHER_APP_CLUSTER=mt1

# === CONFIGURACIÓN DE BACKUP ===
BACKUP_DISK=local
BACKUP_NOTIFICATION_MAIL=admin@sigesa.edu.bo

# === CONFIGURACIÓN DE SEGURIDAD ===
# Cambiar en producción
JWT_SECRET=tu_jwt_secret_muy_seguro_aqui
API_SECRET_KEY=tu_api_secret_key_muy_seguro